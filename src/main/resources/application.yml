server:
  error:
    include-stacktrace: never

spring:
  application:
    name: promecal
  datasource:
    url: jdbc:postgresql://localhost:5432/promecal
    username: postgres
    password: benites1234
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        database-platform: org.hibernate.dialect.PostgreSQLDialect

api:
  security:
    secret: ${JWT_SECRET:secret}
